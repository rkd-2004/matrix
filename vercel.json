{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next",
        "config": {
          "distDir": ".next",
          "installCommand": "pnpm install --no-frozen-lockfile"
        }
      }
    ]
  }